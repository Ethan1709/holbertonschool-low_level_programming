#include "lists.h"

/**
 * print_list - prints all the elements
 * @h: pointer
 * Return: Success
 */

size_t print_list(const list_t *h)
{
	if (str == NULL)
		printf("[%d]" "(%s)", 0, nil);

	return (0);
}
