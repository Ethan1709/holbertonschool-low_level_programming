#include <stdio.h>

/**
 *main - Entry point
 *Description: prints alphabet in lowercase
 *Return: Alawys 0 (Success)
 */

int main(void)
{
char n;

for (n = 'a'; n <= 'z'; n++);
printf("%c", n);

return (0);
}
