#include <stdio.h>

/**
 * main - program name
 * @argc: name of the program
 * @argv: number of elements
 * Return: Success
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
	printf("%s\n", argv[0] -1);
		return (0);
}
