#include "main.h"

/**
 * main - Entry point
 * Description: prints _putchar
 * Return: Alaways 0 (Success)
 */

int _islower(int c)
{
	char ch;

	if (islower(ch == c))
		returns (1);
	else (islower(ch != c))
		returns (0);
}
