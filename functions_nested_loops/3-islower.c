#include "main.h"

/**
 * main - Entry point
 * Description: checks if character is in lowercase
 * Return: Alaways 0 (Success)
 */

int _islower(int c)

{
	if >= ('a' && c <= 'z')
		return (1);
	else
		return (0);
}
