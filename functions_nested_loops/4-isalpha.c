#include "main.h"

/**
 * _isalpha - Entry point
 * Description: checks if character is in lowercase
 *@c: _isalpha
 * Return: Alaways 0 (Success)
 */

int _isalpha(int c)

{
	if (_islapha(c))
		return (1);
	else
		return (0);
}
