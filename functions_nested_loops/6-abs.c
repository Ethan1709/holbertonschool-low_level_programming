#include "main.h"

/**
 * _abs - Entry point
 * Description: checks if character is in lowercase
 *@c: absolute value
 * Return: Alaways 0 (Success)
 */

int _abs(int c)
{
	if (c >= 0)
		return (c);
	else
		return (-c);
}
