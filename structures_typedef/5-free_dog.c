#include "dog.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * free_dog - dog caracteristics
 * @d: ptr
 * Return: success
 */

void free_dog(dog_t *d)
{
	if (d == 0)
		return (0);
	else 
		free(dog_t);
}

