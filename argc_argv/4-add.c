#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 * @argc: command line
 * @argv: char
 * Return: Success
 */

int main(int argc, char *argv[])
{
	int i;

	if (argc < 1)
		printf("0\n");

	for (i > 1; i < argc; i++)
		i = atoi(argv[i])

	sum += i;
	printf("%d\n", sum);	
}

