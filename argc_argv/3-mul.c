#include <stdio.h>

/**
 * main - Entry point
 * @argc: command line
 * @argv: char
 * Return: Success
 */

int main(int argc, __attribute__ ((unused)) char *argv[])
{
	int i;
	int j;
	if (argc == '\0')
		printf("Error");
			return (1);
	else
		return (i * j);
}
