#include "main.h"
#include <stddef.h>
#include <stdlib.h>

/**
 *
 */

char *create_array(unsigned int size, char c)
{
	if (size == 0)
		return NULL;

	int * size = malloc(sizeof(char c));
		_putchar('c');

	return (0);
}
