#include "main.h"
#include <stddef.h>
#include <stdlib.h>

/**
 *
 */

char *create_array(unsigned int size, char c)
{
	if (size == 0)
		return NULL;

	char * create_array = malloc(sizeof(size));
		_putchar('c');
}
