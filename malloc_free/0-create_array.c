#include "main.h"
#include <stddef.h>

/**
 *
 */

char *create_array(unsigned int size, char c)
{
	if (size == 0)
		return NULL;

	for (size >= 1; size < create_array)
	{
		char[size] = "c";
	}
	return (0);
}
